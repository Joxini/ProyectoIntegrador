/* El código exporta una variable constante llamada `entornos` con una propiedad `API_URL` establecida
en el valor ``http://localhost:3000''. Esto permite que otros módulos o archivos importen y utilicen
la variable `entornos` y accedan a su propiedad `API_URL`. */
export const environments{
    API_URL = 'http://localhost:3000'
}